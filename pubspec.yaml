name: urlens_workspace
description: "URLens Workspace - Contains frontend Flutter app and backend FastAPI service"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

# This is a workspace manifest file
# The actual Flutter app is in the frontend/ directory
# The backend API is in the backend/ directory
